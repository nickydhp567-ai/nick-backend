# NICK Bank Backend — environment configuration
# Leave DATABASE_URL unset to use the zero-setup SQLite fallback.

# DATABASE_URL=postgres://user:password@localhost:5432/nick_bank
# PGSSL=true
# SQLITE_PATH=./nickbank.sqlite

PORT=3000
JWT_SECRET=change-this-to-a-long-random-string-in-production
